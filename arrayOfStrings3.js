let d = ["sun", "moon", "earth", "saturn", "rings", "isro", "nasa", "space"];
let searElemt = "isro";
let c = 0;
console.log("The element we are searching '" + searElemt + "' in array of strings " + d + " is at index ")
for (let i = 0; i < d.length; i++) {
    // c = 0;
    // for (let j = 0; j < d[i].length; j++) {
    if (d[i] == searElemt) {
        //     c++;

        // }
        // if (c != 0) {
        console.log(i);
    }
}